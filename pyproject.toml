[project]
name = "rc-2026"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pywebview[gtk]>=6.2.1",
]

[tool.uv.workspace]
members = [
    "clock_app",
]
